# Contracts Changelog

## 0.1.0
- Initial MVP contracts for parameters, feature commands, MCP tools, UI events, rules, and artifact manifests.
- Contract consumers must treat `version` as semantic version text and reject unknown major versions.
